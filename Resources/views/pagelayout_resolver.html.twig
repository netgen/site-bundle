{% set pagelayout = viewbaseLayout is defined ? viewbaseLayout : 'NetgenMoreBundle::pagelayout_empty.html.twig' %}

{% if ( noLayout is not defined or not noLayout ) and ezpublish.configResolver.hasParameter( 'pagelayout', 'ngmore' ) %}
    {% set pagelayout = ezpublish.configResolver.getParameter( 'pagelayout', 'ngmore' ) %}
{% endif %}

{% extends pagelayout %}
